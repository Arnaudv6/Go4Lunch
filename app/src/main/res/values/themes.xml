<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Go4Lunch" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorOnPrimary">@color/orange</item>
        <item name="colorPrimaryVariant">@color/orange_lighter</item>
        <item name="android:windowBackground">@color/white</item>
        <!-- Secondary brand color. I use them on buttons -->
        <item name="colorSecondary">@color/white</item>
        <item name="colorSecondaryVariant">@color/orange_lighter</item>
        <item name="colorOnSecondary">@color/black</item>

        <item name="colorSurface">@color/orange</item>
        <item name="colorOnSurface">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/orange_darker</item>
        <!-- Customize your theme here. -->
        <item name="android:listDivider">@color/orange_lighter</item>

        <item name="colorControlNormal">@color/white</item>
        <item name="android:editTextColor">@color/white</item>
        <item name="titleTextColor">@color/white</item>
    </style>

    <style name="Theme.Go4Lunch.drawer" parent="Theme.Go4Lunch">
        <item name="colorSurface">@color/orange_lighter</item>
        <item name="colorOnSurface">@color/white</item>
    </style>

    <style name="RatingBar" parent="android:style/Widget.Material.RatingBar.Small">
        <item name="colorControlNormal">@color/grey</item>
        <item name="colorControlActivated">@color/gold</item>
    </style>
</resources>
